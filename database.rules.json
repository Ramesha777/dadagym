{
  "rules": {
    "admins": {
      "$uid": {
        ".read": "auth != null && auth.uid === $uid",
        ".write": false
      }
    },

    "adminChats": {
      ".read": "auth != null && (auth.token.admin === true || root.child('admins').child(auth.uid).val() === true)",

      "$trainerUid": {
        ".read": "auth != null && (auth.uid === $trainerUid || auth.token.admin === true || root.child('admins').child(auth.uid).val() === true)",

        "$messageId": {
          ".write": "auth != null && !data.exists() && newData.hasChildren(['senderId', 'senderName', 'senderRole', 'text', 'timestamp']) && newData.child('senderId').val() === auth.uid && newData.child('senderName').isString() && newData.child('senderName').val().length <= 200 && newData.child('text').isString() && newData.child('text').val().length > 0 && newData.child('text').val().length <= 5000 && ((newData.child('senderRole').val() === 'admin' && (auth.token.admin === true || root.child('admins').child(auth.uid).val() === true)) || (newData.child('senderRole').val() === 'trainer' && auth.uid === $trainerUid))"
        }
      }
    }
  }
}
